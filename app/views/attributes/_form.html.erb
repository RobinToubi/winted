<%= form_with(model: attribute) do |form| %>
  <% if attribute.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(attribute.errors.count, "error") %> prohibited this attribute from being saved:</h2>

      <ul>
        <% attribute.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

   <div class="field">
    <%= form.label :vtype %>
    <%= form.select :vtype, options_for_select(Attribute.vtypes.keys.to_a) %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
